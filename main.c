// Votr - voting command line program

#include<stdio.h>
#include<string.h>
#include "issue.h"

int main(int argc, char *argv[]) {
    if (argc == 2) {
        printf("The argument was %s", argv[1]);
    }

    // CANDIDATE c;

    // strcpy(c.name, "Moosebear");
    // strcpy(c.party, "Subscribe to Moosebear Productions");
    // c.id = 1;

    // printcandidate(&c);

    return 0;
}